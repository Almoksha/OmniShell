<UserControl x:Class="OmniShell.Views.Widgets.NotesWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Border x:Name="WidgetBorder" 
            Background="#1E1E38" 
            CornerRadius="10" 
            BorderBrush="#374151"
            BorderThickness="1">
        <StackPanel>
            <!-- Widget Header -->
            <Border Background="#262640" CornerRadius="10,10,0,0" Padding="12,10">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M4,4 L16,4 L20,8 L20,20 L4,20 Z M16,4 L16,8 L20,8" 
                              Stroke="#EC4899" 
                              StrokeThickness="1.5"
                              Width="16" Height="16" 
                              Stretch="Uniform"
                              Margin="0,0,8,0"/>
                        <TextBlock Text="Quick Notes" 
                                   FontSize="13" 
                                   FontWeight="SemiBold"
                                   Foreground="White"/>
                    </StackPanel>
                    <Button x:Name="ClearButton" 
                            Click="ClearButton_Click"
                            HorizontalAlignment="Right"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            ToolTip="Clear notes">
                        <TextBlock Text="Clear" FontSize="11" Foreground="#6B7280"/>
                    </Button>
                </Grid>
            </Border>
            
            <!-- Notes Area -->
            <Border Margin="8" Background="#262640" CornerRadius="6">
                <TextBox x:Name="NotesTextBox"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         VerticalScrollBarVisibility="Auto"
                         Height="120"
                         Background="Transparent"
                         Foreground="#E5E7EB"
                         BorderThickness="0"
                         Padding="10"
                         FontSize="12"
                         CaretBrush="#6366F1"
                         TextChanged="NotesTextBox_TextChanged">
                    <TextBox.Resources>
                        <Style TargetType="TextBox">
                            <Setter Property="SelectionBrush" Value="#6366F1"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
            </Border>
            
            <!-- Character Count -->
            <TextBlock x:Name="CharCountText"
                       Text="0 characters"
                       FontSize="10"
                       Foreground="#6B7280"
                       Margin="12,0,12,8"
                       HorizontalAlignment="Right"/>
        </StackPanel>
    </Border>
</UserControl>
