<UserControl x:Class="OmniShell.Views.Widgets.BatteryWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Border x:Name="WidgetBorder" 
            Background="#1E1E38" 
            CornerRadius="10" 
            BorderBrush="#374151"
            BorderThickness="1">
        <StackPanel>
            <!-- Widget Header -->
            <Border Background="#262640" CornerRadius="10,10,0,0" Padding="12,10">
                <StackPanel Orientation="Horizontal">
                    <Path Data="M4,4 L20,4 C21,4 22,5 22,6 L22,18 C22,19 21,20 20,20 L4,20 C3,20 2,19 2,18 L2,6 C2,5 3,4 4,4 M23,8 L23,16" 
                          Stroke="#10B981" 
                          StrokeThickness="1.5"
                          Width="16" Height="16" 
                          Stretch="Uniform"
                          Margin="0,0,8,0"/>
                    <TextBlock Text="Battery" 
                               FontSize="13" 
                               FontWeight="SemiBold"
                               Foreground="White"/>
                </StackPanel>
            </Border>
            
            <!-- Battery Status -->
            <Grid Margin="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock x:Name="PercentageText" 
                               Text="100%" 
                               FontSize="28" 
                               FontWeight="Bold"
                               Foreground="#10B981"/>
                    <TextBlock x:Name="StatusText" 
                               Text="Plugged in" 
                               FontSize="12" 
                               Foreground="#9CA3AF"
                               Margin="0,4,0,0"/>
                    <TextBlock x:Name="TimeRemainingText" 
                               Text="" 
                               FontSize="11" 
                               Foreground="#6B7280"
                               Margin="0,2,0,0"/>
                </StackPanel>
                
                <!-- Battery Icon -->
                <Border Grid.Column="1" 
                        Background="#0F0F23" 
                        CornerRadius="4" 
                        Width="48" Height="24"
                        VerticalAlignment="Center">
                    <Grid>
                        <Border x:Name="BatteryFill"
                                Background="#10B981"
                                CornerRadius="3"
                                HorizontalAlignment="Left"
                                Margin="2"
                                Width="40"/>
                        <TextBlock x:Name="ChargingIcon"
                                   Text="⚡"
                                   FontSize="12"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Visibility="Collapsed"/>
                    </Grid>
                </Border>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
